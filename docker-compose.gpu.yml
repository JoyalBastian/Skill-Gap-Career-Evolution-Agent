# Optional NVIDIA GPU for the Ollama service (Linux/WSL with NVIDIA Container Toolkit)
# docker compose -f docker-compose.yml -f docker-compose.gpu.yml --profile ollama up --build

services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
